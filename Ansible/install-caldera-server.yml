- hosts: all
  roles:
  - role: install-caldera
    vars:
      dir: '{{ node_app_dir }}/install-caldera/'
#  - role: install-linux-agents
#    vars:
#      dir: '{{ node_app_dir }}/install-linux-agents/'
